% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdalblock-class.R
\name{read_block}
\alias{read_block}
\title{gdalblock S7 Class}
\usage{
read_block(x, ...)
}
\arguments{
\item{x}{A \code{gdalblock} object.}

\item{dsn}{A dataset description (file path, URL, or any GDAL-supported URI).}

\item{i}{Block column index (0-based).}

\item{j}{Block row index (0-based).}

\item{band}{Band number (1-based, default 1).}
}
\value{
A \code{gdalblock} object.

A matrix of raster values.
}
\description{
An S7 class for block-based access to GDAL raster datasets.
}
\details{
The gdalblock class wraps a \code{gdalraster::GDALRaster} object and provides
convenient access to the block (tile) structure of the dataset.
\subsection{Properties}{

Access properties using \code{@}:
\itemize{
\item \code{dsn}: The dataset description/URI
\item \code{bbox}: Bounding box as c(xmin, ymin, xmax, ymax)
\item \code{dimension}: Dimensions as c(ncol, nrow, nbands)
\item \code{crs}: Coordinate reference system (WKT string)
\item \code{nbands}: Number of bands
\item \code{datatype}: Character vector of data types per band
\item \code{res}: Resolution as c(xres, yres)
\item \code{blocksize}: Standard block size as c(width, height)
\item \code{nblocks}: Number of blocks as c(nx, ny)
\item \code{blocks}: List with block structure details
}
}

\subsection{Methods}{
\itemize{
\item \code{read_block(x, i, j, band)}: Read block data as matrix
\item \code{block_dim(x, i, j)}: Get block dimensions
\item \code{block_bbox(x, i, j)}: Get block bounding box
\item \code{block_index(x, i, j)}: Get pixel/line offsets
}
}
}
\examples{
\dontrun{
b <- gdalblock("/path/to/raster.tif")
b@dimension
b@blocksize
read_block(b, 0, 0)
}
Read a block of raster data

}
